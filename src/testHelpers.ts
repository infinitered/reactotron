export const defaultReactotronMock = {
  startTimer: jest.fn(),
  configure: jest.fn(),
  close: jest.fn(),
  connect: jest.fn(),
  send: jest.fn(),
  display: jest.fn(),
  reportError: jest.fn(),
  use: jest.fn(),
  onCustomCommand: jest.fn(),
}
