{
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "copy-internal-deps": "lerna run copy-internal-deps",
    "welcome": "./scripts/welcome.sh",
    "e2e": "npm run bootstrap && npm run build && npm run lint && npm run copy-internal-deps && npm run test"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.26"
  },
  "dependencies": {
    "stringify-object": "^2.4.0"
  }
}
